# Test Plan
Generated by /plan-eng-review on 2026-07-09
Branch: develop
Repo: ispeciadev/SkillNest

## Affected Pages/Routes
- Root Workspace Setup — local developer setup and dependencies installation
- Workspace Development Server — concurrently starting Next.js and Express

## Key Interactions to Verify
- Execute `npm install` at root to verify unified dependency hoisting.
- Execute `npm run setup` to verify environment files creation and Prisma database seeding.
- Execute `npm run dev` to verify backend and frontend start concurrently.

## Edge Cases
- Run `npm run setup` when local PostgreSQL is offline to check for caught database errors and clear fallback instructions.

## Critical Paths
- Unified `npm run dev` server boot, verifying that frontend port `3000` proxies API requests to backend port `3001` cleanly.
